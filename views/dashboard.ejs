<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - <%= user.nickname %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">
                <div class="flex items-center">
                    <div class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center mr-3">
                        <i class="fas fa-chart-line text-white"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-semibold text-gray-900">Dashboard Scrum</h1>
                        <p class="text-sm text-gray-600">Sistema de Gestión Ágil</p>
                    </div>
                </div>
                
                <div class="flex items-center space-x-4">
                    <div class="text-right">
                        <p class="text-sm font-medium text-gray-900"><%= user.nickname %></p>
                        <p class="text-xs text-gray-600"><%= user.grupo %></p>
                    </div>
                    <a href="/logout" class="text-gray-500 hover:text-red-500 transition-colors">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Welcome Section -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
            <div class="flex items-center justify-between">
                <div>
                    <h2 class="text-2xl font-bold text-gray-900 mb-2">
                        ¡Bienvenido, <%= user.nickname %>!
                    </h2>
                    <p class="text-gray-600">
                        Has iniciado sesión correctamente en el sistema.
                    </p>
                </div>
                <div class="text-right">
                    <div class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
                        <i class="fas fa-check-circle mr-1"></i>
                        Sesión Activa
                    </div>
                </div>
            </div>
        </div>

        <!-- User Info Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <!-- Usuario Card -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
                        <i class="fas fa-user text-blue-600 text-xl"></i>
                    </div>
                    <div>
                        <h3 class="font-semibold text-gray-800">Usuario</h3>
                        <p class="text-gray-600 text-sm"><%= user.nickname %></p>
                    </div>
                </div>
            </div>

            <!-- Grupo Card -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mr-4">
                        <i class="fas fa-users text-purple-600 text-xl"></i>
                    </div>
                    <div>
                        <h3 class="font-semibold text-gray-800">Grupo</h3>
                        <p class="text-gray-600 text-sm"><%= user.grupo %></p>
                    </div>
                </div>
            </div>

            <!-- ID Card -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mr-4">
                        <i class="fas fa-id-badge text-green-600 text-xl"></i>
                    </div>
                    <div>
                        <h3 class="font-semibold text-gray-800">ID Usuario</h3>
                        <p class="text-gray-600 text-sm font-mono"><%= user.userId.substring(0, 8) %>...</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Features Preview -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">
                <i class="fas fa-cogs text-gray-600 mr-2"></i>
                Funcionalidades del Sistema
            </h3>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                <div class="p-4 bg-gray-50 rounded-lg">
                    <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center mb-3">
                        <i class="fas fa-history text-blue-600"></i>
                    </div>
                    <h4 class="font-medium text-gray-800 mb-1">Historial de Sprints</h4>
                    <p class="text-sm text-gray-600">Próximamente disponible</p>
                </div>

                <div class="p-4 bg-gray-50 rounded-lg">
                    <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center mb-3">
                        <i class="fas fa-chart-bar text-green-600"></i>
                    </div>
                    <h4 class="font-medium text-gray-800 mb-1">Métricas</h4>
                    <p class="text-sm text-gray-600">Próximamente disponible</p>
                </div>

                <div class="p-4 bg-gray-50 rounded-lg">
                    <div class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center mb-3">
                        <i class="fas fa-columns text-purple-600"></i>
                    </div>
                    <h4 class="font-medium text-gray-800 mb-1">Tablero Scrum</h4>
                    <p class="text-sm text-gray-600">Próximamente disponible</p>
                </div>

                <div class="p-4 bg-gray-50 rounded-lg">
                    <div class="w-10 h-10 bg-yellow-100 rounded-lg flex items-center justify-center mb-3">
                        <i class="fas fa-tasks text-yellow-600"></i>
                    </div>
                    <h4 class="font-medium text-gray-800 mb-1">Tareas</h4>
                    <p class="text-sm text-gray-600">Próximamente disponible</p>
                </div>
            </div>
        </div>

        <!-- Session Info -->
        <div class="mt-8 bg-blue-50 border border-blue-200 rounded-lg p-4">
            <div class="flex items-center">
                <i class="fas fa-info-circle text-blue-500 mr-3"></i>
                <div>
                    <h4 class="font-medium text-blue-800">Información de Sesión</h4>
                    <p class="text-sm text-blue-700 mt-1">
                        Tu sesión está activa y segura. Para cerrar sesión, haz clic en el botón de salida en la esquina superior derecha.
                    </p>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
            <div class="text-center text-sm text-gray-500">
                <p>Sistema Scrum v2.0 - Desarrollado para Dardito</p>
                <p class="mt-1">Sesión iniciada como <strong><%= user.nickname %></strong> del grupo <strong><%= user.grupo %></strong></p>
            </div>
        </div>
    </footer>

    <script>
        // Auto-refresh de la página cada 5 minutos para mantener sesión activa
        setTimeout(() => {
            window.location.reload();
        }, 5 * 60 * 1000);

        // Mostrar hora actual
        function updateTime() {
            const now = new Date();
            const timeString = now.toLocaleTimeString('es-ES');
            console.log('Hora actual:', timeString);
        }
        
        updateTime();
        setInterval(updateTime, 60000); // Cada minuto

        // Confirmación al cerrar sesión
        document.querySelector('a[href="/logout"]').addEventListener('click', (e) => {
            if (!confirm('¿Estás seguro de que quieres cerrar sesión?')) {
                e.preventDefault();
            }
        });
    </script>
</body>
</html>